{
	"name": "tau-monorepo",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"clean": "bun run --workspaces clean",
		"build": "cd packages/tui && bun run build && cd ../ai && bun run build && cd ../agent && bun run build && cd ../coding-agent && bun run build",
		"build:binary": "cd packages/coding-agent && bun run build:binary",
		"check": "biome check --write --error-on-warnings . && bun run check:pinned-deps && bun run check:ts-imports && bun run check:shrinkwrap && tsgo --noEmit && bun run check:browser-smoke",
		"check:browser-smoke": "node scripts/check-browser-smoke.mjs",
		"check:pinned-deps": "node scripts/check-pinned-deps.mjs",
		"check:shrinkwrap": "node scripts/generate-coding-agent-shrinkwrap.mjs --check",
		"check:ts-imports": "node scripts/check-ts-relative-imports.mjs",
		"start": "bun run build && TAU_DEBUG_LOADER=1 TAU_DEBUG_TOOL_RESULT=1 bun packages/coding-agent/dist/cli.js --provider openrouter --model xiaomi/mimo-v2.5-pro",
		"test": "bun run --workspaces test"
	},
	"devDependencies": {
		"@anthropic-ai/sandbox-runtime": "0.0.26",
		"@biomejs/biome": "2.3.5",
		"@types/node": "22.19.19",
		"@typescript/native-preview": "7.0.0-dev.20260120.1",
		"esbuild": "0.28.0",
		"jiti": "2.7.0",
		"pino": "10.3.1",
		"pino-pretty": "13.1.3",
		"shx": "0.4.0",
		"tsx": "4.22.1",
		"typescript": "5.9.3"
	},
	"engines": {
		"node": ">=22.19.0"
	},
	"version": "0.0.3",
	"overrides": {
		"rimraf": "6.1.2",
		"gaxios": {
			"rimraf": "6.1.2"
		}
	}
}
